<%- include("partials/_header") %>

<div class="mt-5">
  <div class="row">
    <div class="col-md-6">
      <table class="table table-dark table-bordered table-hover">
        <thead>
          <tr>
            <td>n°</td>
            <td>volumen</td>
            <td>Grupo</td>
            <td>RH</td>
          </tr>
        </thead>
        <tbody>
          <% if (inventario) { %> <% for(var i = 0; i < inventario.length; i++) {
          %>
          <tr>
            <td><%= (i + 1) %></td>
            <td><%= inventario[i].volumen_total %></td>
            <td><%= inventario[i].grupo %></td>
            <td><%= inventario[i].RH %></td>
          </tr>
          <% } %> <% } %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<%- include("partials/_footer") %>
